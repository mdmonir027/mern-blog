import { makeStyles } from '@material-ui/core';

const useStyles = makeStyles({
  postLink: {
    textDecoration: 'none',
    color: '#000',
    display: 'block',
    marginBottom: '5px',
  },
  postsWrapper: {
    background: '#F5F5F5',
  },
});

export default useStyles;
