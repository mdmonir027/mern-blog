import { makeStyles } from '@material-ui/core';

const useStyles = makeStyles({
  replyForm: {
    marginTop: '20px',
    marginBottom: '20px',
  },
  loginTitle: {
    marginTop: '20px',
    marginBottom: '20px',
    color: 'red',
  },
});

export default useStyles;
